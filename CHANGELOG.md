🛠️ **Lançamento de utilitários!**

Nessa versão já desenvolvemos um certo nível de código, mas ainda nenhuma gameplay. Porém, houve o desenvolvimento de 4 utilitários:

- **AssetResolver:** Resolve caminhos para arquivos estáticos, com base numa tag base. Senão, usa o arquivo padrão.
- **CSSFileLoader:** Realiza o lazy loading de arquivos CSS, permitindo que não haja sobrecarga do sistema.
- **EventBus:** Permite que a engine do jogo se baseie numa arquitetura orientada a eventos, permitindo que o sistema responda a estímulos, ao invés de funções.
- **StringCaseFormatter:** Realiza a formatação de String para diferentes cases através do uso de Regex.
